# Mobile/web: public values bundled at build time. Never put secrets here.
# Android physical device: use the computer LAN address in development, not localhost.
EXPO_PUBLIC_API_URL=http://127.0.0.1:3001
# web | direct (Android APK only) | store (external purchases hidden)
EXPO_PUBLIC_DISTRIBUTION=store
